{
  "name": "@cargo/email",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "smoke": "tsx scripts/mailpit-smoke.ts"
  },
  "dependencies": {
    "@cargo/contracts": "workspace:*",
    "mailparser": "^3.7.4",
    "nodemailer": "^7.0.6",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.6",
    "@types/node": "^24.3.0",
    "@types/nodemailer": "^7.0.1",
    "tsx": "^4.20.5",
    "vitest": "^3.2.4"
  }
}
